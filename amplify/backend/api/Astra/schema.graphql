type ChatHistoryRemote @model @auth(rules: [{allow: owner}]) {
  id: ID!
  uid: Int!
  assistant_type: String!
  chat_message_list: [String]
  fav: Int!
  user_id: ID!
}

type OpenAi @model @auth(rules: [{allow: public}]) {
  id: ID!
  open_ai: String!
}
 